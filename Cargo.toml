[package]
name = "sotb"
version = "0.4.0"
edition = "2024"

[dependencies]
sdl3 = "0.18"

# Web: build SDL3 from source with Emscripten (static). Native keeps system SDL3.
[target.'cfg(target_os = "emscripten")'.dependencies]
sdl3 = { version = "0.18", features = ["build-from-source"] }
